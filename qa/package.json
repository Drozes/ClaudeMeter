{
  "name": "claudemeter-qa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QA pipeline for ClaudeMeter — DOM contract layer (L2)",
  "scripts": {
    "check": "node check-selectors.mjs"
  },
  "dependencies": {
    "jsdom": "^24.1.0"
  }
}
